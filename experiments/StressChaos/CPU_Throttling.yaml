apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: {{ metadata.name }}
  namespace: {{ metadata.namespace }}

spec:
  mode: one
  selector:
{{ spec.selector | to_yaml | indent(4, true) }}
  stressors:
    cpu:
      workers: 1
      load: 100
  duration: "300h"
